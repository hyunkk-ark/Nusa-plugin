# NusaAuth Config
# Autentikasi — otomatis, tanpa config manual Bedrock
auth:
  session-timeout: 3600
  require-login: true
anti-bot:
  enabled: false
  check-name-length: true
  block-patterns: ["_bot", "bot"]
